name: flutter_caffe_ku
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  dartz: ^0.10.1
  dio: ^5.2.0+1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_screenutil: ^5.8.4
  freezed_annotation: ^2.2.0
  get_it: ^7.6.0
  http: ^1.1.0
  pretty_dio_logger: ^1.3.1
  shared_preferences: ^2.1.2

dev_dependencies:
  build_runner: ^2.4.5
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.3.5

flutter:
  uses-material-design: true
